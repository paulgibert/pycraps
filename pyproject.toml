[project]
name = "pycraps"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "gymnasium>=1.2.3",
    "sb3-contrib>=2.7.1",
    "stable-baselines3>=2.7.1",
    "tensorboard>=2.20.0",
    "wandb>=0.24.2",
]

[project.optional-dependencies]
dev = ["pytest", "hypothesis"]

